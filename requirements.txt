websockets==12.0
# TLS: self-signed cert generation for the HTTPS dashboard (secure context
# required for the browser mic / getUserMedia over LAN). Optional at runtime —
# only needed when launching with --tls and no --tls-cert/--tls-key supplied.
cryptography>=42
# Testing
pytest>=8
pytest-asyncio>=0.23
